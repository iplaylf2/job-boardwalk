{
  "extends": "dependency-cruiser/configs/recommended-strict",
  "forbidden": [
    {
      "name": "no-orphans",
      "comment": "Knip owns unused-file detection; directory-level dependency cruising makes every root graph appear orphaned.",
      "severity": "ignore",
      "from": {},
      "to": {}
    }
  ],
  "options": {
    "exclude": {
      "path": "(^|/)dist($|/)"
    },
    "enhancedResolveOptions": {
      "conditionNames": ["import", "require", "node", "default"],
      "exportsFields": ["exports"]
    }
  }
}
