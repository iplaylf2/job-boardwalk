// oxlint-disable-next-line import/no-unassigned-import -- ArkType configuration must execute before its parser is imported.
import "./configure-arktype.ts";

export { type as toolInput } from "arktype";
