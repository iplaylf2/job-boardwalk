{
  "name": "@job-boardwalk/browser-session",
  "private": true,
  "description": "Persistent HTTP MCP service for a project-owned visible Patchright browser.",
  "files": [
    "dist"
  ],
  "type": "module",
  "imports": {
    "#/*": "./src/*"
  },
  "scripts": {
    "build": "vite build && pnpm run deploy",
    "deploy": "pnpm --dir ../.. --force --config.node-linker=hoisted --filter @job-boardwalk/browser-session --prod deploy target/service-artifacts/browser-session",
    "dev": "tsx watch src/main.ts",
    "lint": "oxlint --config oxlint.config.ts .",
    "start": "node dist/index.cjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc -p test/tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "catalog:",
    "@modelcontextprotocol/sdk": "catalog:",
    "@shajara/host": "catalog:",
    "arktype": "catalog:",
    "hono": "catalog:",
    "patchright": "1.61.1"
  },
  "devDependencies": {
    "@job-boardwalk/contracts": "workspace:*",
    "@job-boardwalk/platform-catalog": "workspace:*",
    "@job-boardwalk/presets": "workspace:*",
    "@types/node": "catalog:",
    "oxlint": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
