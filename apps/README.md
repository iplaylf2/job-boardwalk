# Applications

- [`dashboard`](dashboard/) is the local control and status surface presented through SolidJS.
- [`runtime`](runtime/) owns local persistence, agent integration, and platform-browser coordination.

Cross-application behavior is defined in the repository's
[product design](../docs/product-design.md). Each application README documents only that
application's current responsibilities and operation.
