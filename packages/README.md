# Packages

- [`platform-catalog`](platform-catalog/) owns stable recruiting-platform identifiers and display
  labels.
- [`contracts`](contracts/) owns product contracts shared by the runtime and its clients.
- [`storage-layout`](storage-layout/) owns the private filesystem layout and directory permissions
  used by applications that persist local data.
